# personal website (v4)

## Requirements

- Hugo extended
- Go, if you want to use Hugo modules for theme installation

## Template setup

The site uses local layouts in `layouts/` and a single stylesheet in `static/css/site.css`. No external theme module is required.

## Local preview

After installing Hugo extended, run:

```bash
hugo server --disableKinds RSS
```
